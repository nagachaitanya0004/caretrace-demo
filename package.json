{
  "name": "care-trace-ai-demo",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix caretrace-ai/frontend",
    "build": "npm run build --prefix caretrace-ai/frontend",
    "preview": "npm run preview --prefix caretrace-ai/frontend",
    "lint": "npm run lint --prefix caretrace-ai/frontend"
  }
}
